pipeline {
    agent any

    // environment {
    //     GITHUB_TOKEN = credentials('github-gpt') // GitHub personal access token credential ID
    // }

    stages {
        stage('Checkout') {
            steps {
                script {

                    echo "Hello World"
                }
            }
        }

    }
}
